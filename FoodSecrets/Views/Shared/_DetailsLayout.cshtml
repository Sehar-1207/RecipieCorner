
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@Model.Name - Recipe Details</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">

    <style>
        body {
            background-color: #fff;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            color: #333;
            margin: 0;
        }

        /* FIX: Re-introduced a content wrapper to control the overall width and prevent overflow. */
        .recipe-content-wrapper {
            max-width: 1400px; /* Constrains the content to a readable width */
            margin: 0 auto; /* Centers the content on the page */
            padding: 3rem 1rem; /* Adds space on the top/bottom and sides */
        }

        .recipe-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .recipe-title {
            font-size: 2.8rem; /* Slightly reduced for better balance */
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        /* FIX: New styling for the image container per your request */
        .recipe-image-container {
            width: 95%; /* 95% of the parent wrapper */
            height: 500px; /* Fixed height of 500px */
            margin: 0 auto 2.5rem auto; /* Centers the container horizontally and adds bottom margin */
            border-radius: 12px;
            overflow: hidden; /* Ensures the image respects the border-radius */
            background-color: #f0f0f0; /* Fallback color if image is missing */
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* FIX: Ensures the image fills the container perfectly without distortion */
        .recipe-hero-image {
            width: 100%;
            height: 100%;
            object-fit: cover; /* This is the key property for sizing */
        }

        .recipe-meta-bar {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 2rem;
            padding-bottom: 2rem;
            margin-bottom: 2rem;
            border-bottom: 1px solid #eee;
        }

        .meta-item {
            font-size: 1rem;
            color: #555;
        }

            .meta-item i {
                color: #007bff;
                margin-right: 8px;
            }

        .recipe-description {
            font-size: 1.15rem;
            color: #666;
            text-align: center;
            line-height: 1.7;
            margin: 0 auto 3rem auto;
            max-width: 80%; /* Prevents description from being too wide */
        }

        .recipe-section-title {
            font-size: 1.75rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 2px solid #007bff;
            display: inline-block;
        }

        .admin-actions {
            margin-left: auto;
            white-space: nowrap;
        }

        .ingredient-list,
        .instruction-list {
            list-style: none;
            padding-left: 0;
        }

            .ingredient-list li {
                display: flex;
                align-items: center;
                padding: 0.75rem 0;
                border-bottom: 1px solid #f0f0f0;
                font-size: 1.05rem;
            }

                .ingredient-list li:last-child {
                    border-bottom: none;
                }

        .instruction-list {
            counter-reset: instruction-counter;
        }

            .instruction-list li {
                display: flex;
                align-items: flex-start;
                margin-bottom: 1.5rem;
                font-size: 1.05rem;
                line-height: 1.7;
            }

                .instruction-list li::before {
                    counter-increment: instruction-counter;
                    content: counter(instruction-counter);
                    background-color: #007bff;
                    color: white;
                    font-weight: bold;
                    border-radius: 50%;
                    min-width: 32px;
                    height: 32px;
                    display: inline-flex;
                    align-items: center;
                    justify-content: center;
                    margin-right: 15px;
                    flex-shrink: 0;
                }

        .instruction-text {
            flex-grow: 1;
        }

        .rating-card {
            border-top: 1px solid #eee;
            padding: 1.5rem 0;
            display: flex;
        }

            .rating-card:last-child {
                border-bottom: 1px solid #eee;
            }

        .rating-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .rating-user {
            font-weight: 600;
            font-size: 1.1rem;
        }

        .rating-stars {
            color: #ffc107;
        }

        .rating-comment {
            color: #555;
        }

        .final-actions {
            margin-top: 3rem;
        }

        /* FIX: Removed the aggressive CSS that broke Bootstrap's grid padding */
    </style>
</head>
<body>
    <main class="content-area">
        @RenderBody()
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>